DATABASE_URL=mysql+pymysql://usuario:senha@localhost:3306/nome_do_banco

# Webhook do n8n notificado a cada passageiro cadastrado.
# Deixe em branco ou remova a linha para desligar a integração.
# Use a URL de produção (/webhook/), que só responde com o workflow ativo.
N8N_WEBHOOK_URL=
